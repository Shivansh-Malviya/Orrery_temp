# Agents Internal Layer

Internal agent assets. Root `AGENTS.md` remains the discoverable scope bridge.

Subdirs: `rules/`, `skills/`, `workflows/`, `protocols/`, `vscode/`.
Do not restore `.agents/AGENTS.md`.
