# Workspace

Workspace contains product OSes, domain workspaces, routing spaces, and independent project locations.

## Classes

- Product OS: `lattice`, `argos`, `work`
- Domain workspace: `research`, `education`, `me`
- Routing workspace: `inbox`
- Project namespace: `side-projects`

Do not keep generic domain folders active when a product OS already owns that domain.
