# Career Compatibility Bridge

Career/application memory is now canonical in `../argos/`.

This folder is retained only as a compatibility bridge during migration. Do not add new canonical application/campaign material here.
