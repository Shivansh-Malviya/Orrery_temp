# WorkBrain Workspace

Restricted work-domain memory and execution space.

Do not export, summarize, or route work source material outside this workspace unless explicitly approved.
Only `08_shareable_exports/` is export-safe by default.
