# Orrery Actual-Repo Structural Pass

Source remote: `Shivansh-Malviya/Orrery`
Source commit: `7450fb9b6ff79ebfb25661eda387560e3f1263a6`

This package was built from the current live remote state observed at the source commit and then corrected according to the federated Orrery architecture plan.

## Applied changes

- Promoted `pd` to `lattice` as first-class knowledge product.
- Promoted ACOS/ArgOS to `workspace/argos` as first-class application/campaign OS.
- Added restricted `workspace/work` WorkBrain structure.
- Converted `SECOND_BRAIN` into lowercase `second-brain` general synthesis layer.
- Converted root `.agents/Protocols` into `.agents/protocols`.
- Removed root `.agents/AGENTS.md` pattern in favor of `.agents/README.md`.
- Converted governance into docs-only surface.
- Removed unused `vaults/` surface from the corrected structure.
- Added `tools/registry.md` and tool adapter/shim surfaces.
- Preserved 9-file designator protocol naming.

## Remote mutation

No remote changes were pushed.
