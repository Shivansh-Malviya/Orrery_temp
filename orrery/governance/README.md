# Governance

Governance is docs-only.

It defines Orrery's principles, operating model, lifecycle, and review gates.
Runtime implementation lives in `../system/`.
Authoritative rules live in `../policy/`.
Executable triggers live in `../hooks/`.
